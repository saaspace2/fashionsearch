# Databricks bundle state — machine-specific, never commit
.databricks/
build/
dist/
*.egg-info/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Notebooks
.ipynb_checkpoints/

# Local secrets — if this ever gets committed, rotate the token immediately
.env
*.pem
